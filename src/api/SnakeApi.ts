export class Snake {
    constructor() {}

    Collect(direction) {
    }

    Attack(direction) {
    }

    Heal(direction) {
    }

    Move(direction) {
    }

    Watch(direction) {
    }

    Run() {}
}