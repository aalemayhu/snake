module.exports = function Next(views, sViews, body) {
    const treatMatch = views.find(v => { return v == "treat"});
    if (treatMatch) {
      return treatMatch.direction;
    }
    let v = views.map(v => v.contains !== "player" && v.contains !== "wall");
    return v[~~(Math.random() * v.length)];
};
